---
deployment:
  tasks:
    # PRODUCȚIE: deploy pe rădăcina games.ro (validat pe canar 2026-08-09).
    # Rollback: revert la commit-ul anterior + Update/Deploy, sau restaurare JetBackup.
    # directii-preview/ NU se copiază (doar site/). Canarul /test-fabrica/ rămâne înghețat.
    - export DEPLOYPATH=/home/gamesro/public_html/
    - /bin/cp -R site/. $DEPLOYPATH
